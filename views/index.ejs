<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Planet Movies</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-orange-400 to-purple-800 text-white h-screen flex">
   


    <!-- Movie Details -->
    <div class="w-3/4 flex  justify-center items-center p-6">
        <form action="/send" method="post" class=" m-10 flex items-center justify-center mt-6">
            <input type="text" name="title" 
                class="w-80 text-yellow-400 px-4 py-2 border  border-gray-300 rounded-l-md focus:outline-none focus:ring-2 focus:ring-red-500"
                placeholder="Search for a movie..." required>
            <button type="submit" 
                class="px-4 py-2 bg-red-600 text-white font-semibold rounded-r-md hover:bg-red-700 transition">
                Search
            </button>
        </form>
        <% if(locals.data) {%>
        <div class="bg-purple-900 p-6 rounded-2xl w-96">
            <img src="<%= data.Poster %>" alt="<%= data.Poster %>" class="w-full h-48 object-cover rounded-xl">
            <h2 class="text-xl font-bold mt-4"><%= data.Title %></h2>
            <p class="text-sm opacity-80"><%= data.Plot %></p>
            
            <!-- Ratings -->
            <div class="flex justify-between items-center mt-4">
                <span class="text-yellow-400 font-bold">IMDB: <%= data.imdbRating %></span>
                <span class="text-gray-300 text-sm">Year: <%= data.Year %></span>                
            </div>
            <span class="text-red-300 font-bold">Country: <%= data.Country %></span>


            <!-- Genres -->
            <div class="mt-4 flex flex-wrap gap-2">
             
                    <span class="bg-red-600 px-2 py-1 text-sm rounded">Advanture Action</span>
            </div>

            <!-- Actors -->
            <p class="mt-3 text-sm">Starring: <%= data.Actors %></p>

        </div>
       <% } %>
    </div>

</body>
</html>
